const TodoItem = ({ todo, onToggle, onRemove}) => {
    return (
        <div>
            <input type="checkbox" />
            <span>test text</span>
            <button>del</button>
        </div>
    );
};

const Todos = ({})